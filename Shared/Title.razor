@using HalmaEditor.Data
@inject TitleData data
<title>@data.Title</title>
@code {

    protected override void OnInitialized()
    {
        data.TitleUpdated += (sender, e) => this.InvokeAsync(this.StateHasChanged);
    }
}
